package esl;

import java.util.Scanner;

public class charr {

	public static void main(String[] args) {
		String name;

		Scanner scan = new Scanner(System.in);
		System.out.println("Enter your name");
		name = scan.nextLine();
		System.out.println("Do you have a US driver license?");
		String license = scan.next();
		if (license.contentEquals("no")) {
			System.out.println("Invalid data!");
			System.exit(0);

		}
		String vehicleOwnership;
		int zipcode;
		int milesToWorkOrSchool = 0;
		int age=-0;
		int experince=0;
		String vehicleUsage = "";
		String driveSchollOrHome = " ";
		System.out.println("Write your zip code");
		zipcode = scan.nextInt();

		
		  intzipcode1= (zipcode == 20910 || zipcode == 20740)?
		 // (zipcode == 22102 || zipcode == 22103)? +60 : + 30;
		 
		/*( zipcode == 22102 || zipcode == 22103) { // + 30);
		 * 
		 * }else // + 50);
		 */


          System.out.println("Is this vehicle owned,Financed, or Lease");
		  
		vehicleOwnership = scan.next();

		/*
		 * if (vehicleOwnership.equalsIgnoreCase("owner")) { // + 10); } else { // +
		 * 20);
		 */
		System.out.println("How is this vehicle primarily used?");
		vehicleUsage = scan.next();
		/*
		 * if (vehicleUsage.equalsIgnoreCase("Business")) {
		 * System.out.println(vehicleUsage + 50); } else if
		 * (vehicleUsage.equalsIgnoreCase("Pleasure")) { // + 10);
		 * 
		 * }else if (vehicleUsage.equalsIgnoreCase("Commute")) {
		 * 
		 * // + 20); }
		 */
		System.out.println("Days Driven To Work And/Or School");
		driveSchollOrHome = scan.next();

		// if (driveSchollOrHome.equalsIgnoreCase("work")) {
		// + 5);
		System.out.println("Miles Driven To Work And/or School");
		milesToWorkOrSchool = scan.nextInt();
////////////////////////////////////////////////////////////////////////////////////////
		System.out.println("How old are you?");
		age=scan.nextInt();
		
	/*	if(age>16) {
			System.out.println("Invalid data!");
			System.exit(0);
		}else if(age>16 || age<18) {
			//*20
		}else if(age >= 18  || age <= 21) {
			//*6
		}else  if (age>21 || age<25) {*/
			
			
		System.out.println("How many years you've been driving?");
		experince=scan.nextInt();
		//if (experince>0)
		if(experince==0) {
			System.out.println("invalid data!");
			System.exit(0);
		
		
		
			
		}
		
		
		
		
		
	}
}
